/* pages/index/index.wxss */
/* 一味鲜 UI - 浅色模式（更精美版） */

page {
  --primary-green: #4A7C59;
  --primary-green-dark: #3D6B4A;
  --gold-accent: #8B7355;
  --bg-light: #F5F3EF;
  --bg-white: #FFFFFF;
  --text-dark: #333333;
  --text-gray: #888888;
  --text-light: #AAAAAA;
  --card-radius: 20rpx;
  --shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
}

.page-container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: var(--bg-light);
  overflow: hidden;
}

/* Vibe Premium Header - 新中式轻奢头部 */
.header-container {
  position: relative;
  background: linear-gradient(180deg, #E6F0EB 0%, #FFFFFF 100%);
  padding: 20rpx 40rpx 40rpx;
  overflow: hidden;
}

/* 背景纹理/装饰球 */
.header-bg-decoration {
  position: absolute;
  top: -80rpx;
  right: -60rpx;
  width: 300rpx;
  height: 300rpx;
  background: radial-gradient(circle, rgba(74, 124, 89, 0.08) 0%, rgba(255,255,255,0) 70%);
  border-radius: 50%;
  z-index: 0;
}

/* 品牌区域 */
.brand-section {
  position: relative;
  z-index: 1;
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 30rpx;
}

.brand-name-box {
  display: flex;
  align-items: center;
  margin-bottom: 12rpx;
}

.brand-name {
  font-size: 56rpx;
  font-weight: 900;
  color: #2C3E30;
  font-family: "STKaiti", "KaiTi", "楷体", serif;
  letter-spacing: 4rpx;
  margin-right: 16rpx;
}

/* 红色印章 */
.brand-seal {
  background: #B83B3B;
  color: #fff;
  font-size: 20rpx;
  padding: 4rpx 8rpx;
  border-radius: 6rpx;
  font-weight: lighter;
  font-family: serif;
  box-shadow: 0 2rpx 4rpx rgba(184, 59, 59, 0.3);
  writing-mode: vertical-rl; /* 竖排文字 */
  text-orientation: upright;
  letter-spacing: 2rpx;
}

.brand-info {
  display: flex;
  align-items: center;
  margin-bottom: 10rpx;
}

.shop-status {
  background: rgba(74, 124, 89, 0.1);
  color: var(--primary-green);
  font-size: 20rpx;
  padding: 2rpx 10rpx;
  border-radius: 8rpx;
  margin-right: 16rpx;
  font-weight: 500;
}

.shop-distance {
  font-size: 22rpx;
  color: var(--text-gray);
}

.brand-tags {
  display: flex;
  align-items: center;
  font-size: 22rpx;
  color: #888;
}

.brand-right-logo {
  font-size: 60rpx;
  opacity: 0.9;
  filter: drop-shadow(0 4rpx 8rpx rgba(0,0,0,0.1));
}

/* 悬浮搜索框 (Glassmorphism) */
.search-box-glass {
  position: relative;
  z-index: 2;
  height: 80rpx;
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(10px);
  border-radius: 40rpx;
  display: flex;
  align-items: center;
  padding: 0 10rpx 0 30rpx;
  box-shadow: 0 8rpx 20rpx rgba(0, 0, 0, 0.05);
  border: 1rpx solid rgba(255, 255, 255, 0.6);
}

.search-icon {
  font-size: 28rpx;
  color: #999;
  margin-right: 20rpx;
}

.search-swiper {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx;
}

.placeholder-text {
  font-size: 26rpx;
  color: #888;
}

.search-btn {
  background: var(--primary-green);
  color: #fff;
  font-size: 24rpx;
  padding: 10rpx 24rpx;
  border-radius: 30rpx;
  font-weight: bold;
}

/* 内容区 */
.main-content {
  flex: 1;
  display: flex;
  overflow: hidden;
  padding-bottom: 20rpx;
}

.category-nav {
  width: 170rpx;
  background: transparent;
}

.category-item {
  position: relative;
  padding: 30rpx 10rpx;
  font-size: 28rpx;
  color: var(--text-gray);
  text-align: center;
}

.category-item.active {
  color: var(--text-dark);
  font-weight: bold;
  background: var(--bg-white);
  border-top-right-radius: 30rpx;
  border-bottom-right-radius: 30rpx;
}

.category-item.active::before {
  content: '';
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 8rpx;
  height: 40rpx;
  background: var(--primary-green);
  border-radius: 4rpx;
}

.goods-list {
  flex: 1;
  padding: 0 20rpx;
}

.section-title {
  font-size: 30rpx;
  font-weight: bold;
  color: var(--text-dark);
  padding: 20rpx 0;
}

/* 商品卡片 */
.goods-item {
  display: flex;
  background: var(--bg-white);
  border-radius: var(--card-radius);
  margin-bottom: 20rpx;
  padding: 20rpx;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.04);
}

.goods-image {
  width: 170rpx;
  height: 170rpx;
  border-radius: 16rpx;
  flex-shrink: 0;
  background: #EEE;
}

.goods-info {
  flex: 1;
  margin-left: 20rpx;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.goods-name {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}

.goods-desc {
  font-size: 22rpx;
  color: #999;
  margin-top: 6rpx;
}

.goods-bottom {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.goods-price {
  color: var(--gold-accent);
}

.price-value {
  font-size: 40rpx;
  font-weight: bold;
}

.add-cart-btn {
  background: var(--primary-green);
  color: #fff;
  padding: 10rpx 20rpx;
  border-radius: 30rpx;
  font-size: 22rpx;
  display: flex;
  align-items: center;
}

.add-cart-icon {
  margin-right: 6rpx;
}

/* 加减器 */
.quantity-control {
  display: flex;
  align-items: center;
  background: var(--primary-green);
  padding: 6rpx 16rpx;
  border-radius: 30rpx;
}

.action-btn {
  color: #fff;
  font-size: 36rpx;
  padding: 0 10rpx;
  font-weight: bold;
}

.quantity {
  color: #fff;
  margin: 0 15rpx;
  font-weight: bold;
}

/* 🚀 浮动结算条 - 沉浸式沉页脚版 🚀 */
.float-cart-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 999;
  opacity: 0;
  pointer-events: none;
  transform: translateY(100%);
  transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1);
  background: rgba(45, 45, 45, 0.95);
  backdrop-filter: blur(20px);
  padding-bottom: env(safe-area-inset-bottom); /* 适配全面屏底部黑条 */
  box-shadow: 0 -10rpx 40rpx rgba(0, 0, 0, 0.2);
}

.float-cart-bar.show {
  opacity: 1;
  pointer-events: auto;
  transform: translateY(0);
}

.cart-content {
  height: 120rpx;
  display: flex;
  align-items: center;
  padding: 0 30rpx;
}

.cart-icon-box {
  position: relative;
  width: 110rpx;
  height: 110rpx;
  background: linear-gradient(135deg, var(--primary-green) 0%, var(--primary-green-dark) 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: -60rpx; /* 进一步上提，更有张力 */
  box-shadow: 0 8rpx 30rpx rgba(74, 124, 89, 0.6);
  border: 4rpx solid rgba(255, 255, 255, 0.1);
}

.cart-emoji {
  font-size: 48rpx;
  filter: drop-shadow(0 2rpx 4rpx rgba(0,0,0,0.2));
}

.cart-count-badge {
  position: absolute;
  top: -4rpx;
  right: -4rpx;
  background: #FF4757;
  color: #fff;
  font-size: 24rpx;
  min-width: 40rpx;
  height: 40rpx;
  border-radius: 20rpx;
  text-align: center;
  line-height: 40rpx;
  font-weight: 800;
  border: 4rpx solid #333; /* 与深色背景融合 */
  box-shadow: 0 4rpx 8rpx rgba(0,0,0,0.2);
}

.cart-price-info {
  flex: 1;
  margin-left: 24rpx;
  display: flex;
  flex-direction: column;
}

.price-row {
  color: #fff;
  display: flex;
  align-items: baseline;
}

.symbol {
  font-size: 26rpx;
  font-weight: 400;
  margin-right: 6rpx;
  color: var(--primary-green-light);
}

.total-price {
  font-size: 46rpx;
  font-weight: 700;
  letter-spacing: -1rpx;
}

.delivery-tip {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.5);
  margin-top: -2rpx;
}

.checkout-btn {
  background: linear-gradient(100deg, var(--primary-green-light) 0%, var(--primary-green) 100%);
  color: #fff;
  height: 86rpx;
  line-height: 86rpx;
  padding: 0 50rpx;
  border-radius: 43rpx;
  font-weight: 700;
  font-size: 32rpx;
  box-shadow: 0 4rpx 15rpx rgba(74, 124, 89, 0.3);
  transition: all 0.3s;
  overflow: hidden;
  position: relative;
}

.checkout-btn:active {
  transform: scale(0.96);
  opacity: 0.9;
}


/* 购物车弹出层样式 */
.cart-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.6);
  z-index: 900;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s;
}

.cart-mask.show {
  opacity: 1;
  visibility: visible;
}

.cart-popup {
  position: fixed;
  left: 0;
  right: 0;
  bottom: calc(env(safe-area-inset-bottom) + 120rpx); /* 完美贴合结算条高度 */
  background: var(--bg-white);
  border-radius: 48rpx 48rpx 0 0;
  z-index: 950;
  transform: translateY(120%);
  transition: all 0.5s cubic-bezier(0.19, 1, 0.22, 1);
  padding-bottom: 40rpx;
  box-shadow: 0 -10rpx 40rpx rgba(0, 0, 0, 0.1);
}

.cart-popup.show {
  transform: translateY(0);
}

/* 顶部指示条 */
.cart-popup::after {
  content: '';
  position: absolute;
  top: 12rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 80rpx;
  height: 8rpx;
  background: #E0E0E0;
  border-radius: 4rpx;
}

.popup-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 40rpx 40rpx 30rpx;
  background: #FFFFFF;
  border-radius: 48rpx 48rpx 0 0;
}

.popup-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #333;
  position: relative;
}

.popup-title::after {
  content: '';
  position: absolute;
  bottom: -8rpx;
  left: 0;
  width: 40rpx;
  height: 4rpx;
  background: var(--primary-green);
  border-radius: 2rpx;
}

.clear-cart {
  font-size: 26rpx;
  color: #999;
  display: flex;
  align-items: center;
  background: #F5F5F5;
  padding: 10rpx 20rpx;
  border-radius: 12rpx;
  transition: all 0.2s;
}

.clear-cart:active {
  background: #EEEEEE;
  color: #666;
}

.popup-list {
  max-height: 650rpx;
  padding: 0 40rpx;
}

.cart-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 36rpx 0;
  border-bottom: 1rpx solid rgba(0,0,0,0.05);
}

.cart-item-info {
  flex: 1;
}

.cart-item-name {
  font-size: 30rpx;
  color: #222;
  font-weight: 600;
}

.cart-item-price {
  font-size: 32rpx;
  color: var(--gold-accent);
  font-weight: 700;
  margin-right: 32rpx;
  font-family: 'DIN Alternate', sans-serif;
}

.popup-footer {
  height: 60rpx;
}

